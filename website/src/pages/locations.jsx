import React from 'react'
import { Layout, LocationsPage } from 'shared-components'

const HardcodedLocations = () => (
  <Layout classNamePage="classPageNonStandard classPagePrivacy">
    <LocationsPage />
  </Layout>
)

export default HardcodedLocations
