import { SiteWideTokens } from 'base-components'

const contentWidthLimit = 1400

const siteWide: SiteWideTokens = {
  // Content Width
  contentWidthPx: `${contentWidthLimit}px`,
  contentWidthValue: contentWidthLimit,
}

export default siteWide
