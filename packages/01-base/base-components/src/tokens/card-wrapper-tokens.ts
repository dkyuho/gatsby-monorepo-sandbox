import { coloursBase, CardWrapperTokens } from 'base-components'

const cardWrapperTokens: CardWrapperTokens = {
  cardBgCol: coloursBase.white,
  cardBorder: `1px solid ${coloursBase.black15}`,
  cardBoxShadow: '1px 1px 3px 0 rgba(0, 0, 0, 0.1)',
  cardBorderRadius: '',
}
export default cardWrapperTokens
